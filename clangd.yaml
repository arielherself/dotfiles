CompileFlags:
  # Add: [-fexperimental-new-constant-interpreter]
  Compiler: g++

Diagnostics:
  ClangTidy:
    Add: [bugprone-*,performance-*]
    Remove: [bugprone-reserved-identifier,bugprone-easily-swappable-parameters]

Index:
  Background: Skip

InlayHints:
  BlockEnd: Yes

Hover:
  ShowAKA: Yes
